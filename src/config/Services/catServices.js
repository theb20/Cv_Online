const catService = {
  getCat: async () => { throw new Error("API removed"); },
  getCatById: async (id) => { throw new Error("API removed"); },
  createCat: async (catData) => { throw new Error("API removed"); },
  updateCat: async (id, catData) => { throw new Error("API removed"); },
  deleteCat: async (id) => { throw new Error("API removed"); },
}
export default catService;
