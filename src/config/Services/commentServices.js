const commentService = {
  getComment: async () => { throw new Error("API removed"); },
  getCommentById: async (id) => { throw new Error("API removed"); },
  createComment: async (commentData) => { throw new Error("API removed"); },
  updateComment: async (id, commentData) => { throw new Error("API removed"); },
  deleteComment: async (id) => { throw new Error("API removed"); },
}
export default commentService;
