const likeService = {
  getLike: async (id_comment) => { throw new Error("API removed"); },
  createLike: async (likeData) => { throw new Error("API removed"); },
  deleteLike: async (id) => { throw new Error("API removed"); },
}
export default likeService;
