const skillService = {
  getSkill: async () => { throw new Error("API removed"); },
  getSkillById: async (id) => { throw new Error("API removed"); },
  createSkill: async (skillData) => { throw new Error("API removed"); },
  updateSkill: async (id, skillData) => { throw new Error("API removed"); },
  deleteSkill: async (id) => { throw new Error("API removed"); },
}
export default skillService;
