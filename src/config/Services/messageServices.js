const messageService = {
  getMessage: async () => { throw new Error("API removed"); },
  getMessageById: async (id) => { throw new Error("API removed"); },
  createMessage: async (messageData) => { throw new Error("API removed"); },
  deleteMessage: async (id) => { throw new Error("API removed"); },
}
export default messageService;
