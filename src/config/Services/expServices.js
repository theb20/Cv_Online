const expServices = {
  getExp: async () => { throw new Error("API removed"); },
  getExpById: async (id) => { throw new Error("API removed"); },
  createExp: async (expData) => { throw new Error("API removed"); },
  updateExp: async (id, expData) => { throw new Error("API removed"); },
  deleteExp: async (id) => { throw new Error("API removed"); },
}
export default expServices;
