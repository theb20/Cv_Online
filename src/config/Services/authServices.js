const authService = {
    getAuth: async () => { throw new Error("API removed"); },
    getUserById: async (id) => { throw new Error("API removed"); },
    logout: async () => { throw new Error("API removed"); },
    login: async (credentials) => { throw new Error("API removed"); },
    updateUser: async (id, userData) => { throw new Error("API removed"); },
    deleteUser: async (id) => { throw new Error("API removed"); },
    register: async (userData) => { throw new Error("API removed"); },
    getMe: async () => { throw new Error("API removed"); }
};
export default authService;
